# paginator 

### discrible 

A simple paginator plugin depend on jquery, would be use like bolow:

```
	paginator({
		selector: '.page',          
		page: 1,                    
		total: 120,                
		pageSize: 10,              
		showPage: 10,              
		callback: function(page){}  
	});

```